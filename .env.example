# =============================================================================
# landing/ — environment template. Copy to `.env.local`:
#   cp .env.example .env.local
# `.env*.local` is gitignored. Landing is standalone (no DB, no web/ secrets).
# =============================================================================

# Resend — waitlist + suggestion emails (app/api/{waitlist,suggestions}).
RESEND_API_KEY=""
# Optional: Resend audience to add waitlist signups to.
RESEND_AUDIENCE_ID=""
